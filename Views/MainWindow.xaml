<Window x:Name="HikerMainWindow" x:Class="Hiker_Editor.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Hiker_Editor.Views"
        mc:Ignorable="d"
        Title="Hiker" Height="450" Width="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="18"></RowDefinition>
            <RowDefinition></RowDefinition>
            <RowDefinition></RowDefinition>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="0.3*"></ColumnDefinition>
            <ColumnDefinition Width="*"></ColumnDefinition>
        </Grid.ColumnDefinitions>

        <Menu Height="18" VerticalAlignment="Top" Grid.ColumnSpan="2">
            <MenuItem Header="File">
                <MenuItem Header="New Project" ></MenuItem>
                <MenuItem Header="Open Project" ></MenuItem>
                <MenuItem Header="Save Project" ></MenuItem>
                <Separator />
                <MenuItem Header="Exit" ></MenuItem>
            </MenuItem>
            <MenuItem Header="Project" >
                <MenuItem Header="Run Game" ></MenuItem>
                <MenuItem Header="Build Project" ></MenuItem>
                <MenuItem Header="Setting Project" ></MenuItem>
            </MenuItem>
            <MenuItem Header="Help" >
                <MenuItem Header="Documentation" ></MenuItem>
                <Separator />
                <MenuItem Header="About" Command="{Binding OpenAbout}"></MenuItem>
            </MenuItem>
        </Menu>

        <TreeView x:Name="StructureProject" Grid.Row="1" Grid.RowSpan="2" ItemsSource="{Binding StructureProject}">
            <TreeView.ItemTemplate>
                <HierarchicalDataTemplate ItemsSource="{Binding Path=Items}">
                    <TextBlock>
                        <Image Source="{Binding ImagePath}" Width="12" Height="12" />
                        <TextBlock Text="{Binding Name}">
                            <TextBlock.ContextMenu>
                                <ContextMenu ItemsSource="{Binding Path=ItemsOperation}"/>
                            </TextBlock.ContextMenu>
                        </TextBlock>
                    </TextBlock>
                </HierarchicalDataTemplate>
            </TreeView.ItemTemplate>
        </TreeView>
    </Grid>
</Window>
